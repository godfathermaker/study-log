# tutor-planner — 에이전트 지침

과외 학생(아이)·부모·과외선생 3계정 학습관리 웹앱.
GitHub Pages + Apps Script + 구글시트.
트리거: **"학습관리 앱"** / **"과외 앱"**.

## 시작 전 필수
`README.md` 가 **단일 진실원천**이다. 현재 상태·미완료·검증 결과가 전부 거기 있다.
먼저 읽어라. 단, README 의 수치는 과거 측정값이다 — 현재 상태가 중요하면 다시 재라.

## 이 앱의 존재 이유는 누적이다
3개월 뒤에 "5월엔 학원 주 6시간·자습 2시간, 피드백에 '숙제 미제출' 4번" 을 볼 수
있어야 한다. 스키마를 건드릴 때는 **과거 기록이 깨지는가**를 먼저 물어라.

- **어느 표에서도 행을 물리적으로 삭제하지 않는다.** `삭제` 열에 묘비를 세운다.
- **학원은 지우지 않고 `상태='종료'`.** 지우면 과거 기록의 `학원rid` 가 전부 고아가 된다.
- **`일정` 을 고치지 않는다.** 옛 행을 어제까지로 닫고 새 행을 오늘부터 연다.
  고치면 과거 일과표가 전부 소급 재작성된다.

## 격리는 서버에서 한다 — 절대 클라이언트로 내리지 마라
아이↔부모는 서로의 것을 못 본다. 그 판정은 `appsscript_academy.gs` 의
`doGet`/`doPost` 에서만 한다. 읽을 수 없는 표는 **조회조차 하지 않아** 응답 payload 에
키가 존재하지 않는다. `index.html` 의 `PERM` 은 화면 정리용 사본일 뿐이다 —
거기만 고치고 격리했다고 말하지 마라.

`authOK_` 는 **fail-closed** 다(CPX 와 정반대). 명단을 못 읽으면 아무도 통과 못 한다.
역할을 모르는 채 통과시키면 아이가 부모 데이터를 받아간다. 이 방향을 뒤집지 마라.

## 동기화는 CPX 하드닝의 이식본이다
`~/claude/cpx-flashcards-web/` 에서 왔다. 아래를 제거하면 그쪽에서 겪은 유실이 재현된다.

- 부팅·온라인복귀·탭복귀·2분주기 `retryPending()` ← **영구 손실 경로**
- 청크 **순차** 전송 (`Promise.all` 금지)
- 확인은 **응답 도착 기준** (고정 타임아웃 금지)
- `AUTH_WAIT=25000` (콜드스타트 13~21초)
- **오프라인이면 verify 하지 않는다** (헛경고)
- 모든 행에 클라이언트 생성 `rid` + 업서트 — no-cors 라 성공을 모르므로
  이게 없으면 재전송이 전부 중복 행이 된다
- localStorage 키에 **역할** 포함 + `loadLocal()` 이 먼저 **비우고** 읽는다
- `put()` 은 **바뀐 열만** 보낸다. 행 전체를 보내면 서버의 "보낸 열만 갱신" 이
  무의미해지고, 캐시가 낡은 기기가 남의 최신 값을 조용히 되돌린다
- `pend` 는 **무엇을 보냈는지**(`d`)를 들고 있다. 재전송이 행 전체를 보내면 위 문제가 되살아난다
- 어른(parent/teacher) 코드는 기기에 저장하지 않는다 — 공용 기기 보호
- `pull()` 은 못 읽는 표를 캐시에서 **지운다** (구버전 캐시 잔존 방지)
- `lsSet` 실패를 삼키지 않는다. 1차 저장소가 조용히 죽으면 안 된다

## 배포 규율
- 클라이언트와 Apps Script 는 **버전이 짝**이다. 한쪽만 올리지 마라.
  Apps Script 응답의 `v` 마커로 실제 반영본을 확인한다.
- 여러 번 올리지 않는다. `tools/fake_sheet_server.py`(락·지연·콜드스타트 모사) +
  실제 브라우저로 증상을 재현한 뒤 한 번에 배포한다.
- Apps Script 재배포 뒤에는 `tpSetup()` 을 한 번 실행한다.

## 권한 — 보호선은 하나뿐이다
**아이는 피드백·성적을 못 본다.** 그것뿐이다. 부모와 선생은 같은 것을 본다.
학습기록은 아이가 쓰고 어른은 **읽기만** 한다.
세 곳(`appsscript_academy.gs` PERM, `index.html` PERM+VIEWS, `fake_sheet_server.py` PERM)이
항상 같아야 한다. 하나만 고치면 격리가 깨지거나 화면이 어긋난다.
**권한을 건드렸으면 `python3 tools/check_perm.py` 를 먼저 돌려라.**

## 학교는 학원이다
별도 개념이 없다. `학원` 표의 한 행이고 이름만 다르다. 추가 시트의 빠른 시작 버튼이
`학교`(월~금 08:20–15:00)를 채워줄 뿐이다. **`종류` 열을 만들지 마라** — 사용자가
"학원이랑 동일하게 넣고 이름만 바꾸면 된다"고 명시적으로 정했다.
그래서 분석의 "수업 시간"은 학교를 포함한다. 라벨을 "학원 시간"으로 되돌리지 마라.

## 알려진 함정
- **콜드 스타트**: 유휴 뒤 첫 요청 13~21초. 웜(2.6초) 기준으로 타이머를 잡으면
  정상 저장에도 빨간 경고가 뜨고, 전량 재전송이 락 경쟁을 키우는 악순환이 된다.
- **자동 태그를 그대로 저장하지 마라.** 카톡 파서 결과는 반드시 사람이 고칠 수 있는
  폼을 거친다. 틀린 태그가 3개월 쌓이면 분석이 오염되고 되돌릴 수 없다.
- 브라우저 자동화로 한글을 타이핑하면 조용히 실패한다. 값을 직접 넣거나 ASCII 로.
- 분석 화면에서 **인과 주장 금지.** 숫자를 나란히 놓을 뿐이다.

<!-- BEGIN:kmy-common -->
## 공통 작업 규칙 (kmy)

이 블록은 `~/claude/_agents-md/COMMON.md` 가 원본이다. 여기서 고치지 말고 원본을
고친 뒤 `bash ~/claude/_agents-md/sync.sh` 를 돌려라. 직접 편집분은 덮어써진다.

**언어**: 한국어로 답한다. 장황함 지양, 수치·표·ASCII 도식 선호.

**근거 없는 수치 금지**
- 모든 수치 주장에 데이터 소스와 계산 방식을 밝힌다. "~일 것 같다"는 허용 안 됨.
- 문서·메모에 적힌 숫자를 쓸 때는 **언제 측정된 값인지** 함께 쓴다. 현재 상태가
  중요하면 다시 재라. 옛 숫자 위에 추론을 쌓으면 추론 전체가 무너진다.
- 성능 수치는 측정 조건(웜/콜드, 표본 수)을 반드시 병기한다.

**누락 금지 — 최우선**
- 항목 N건을 처리할 때 N은 화면(DOM)이 아니라 **데이터 모델**에서 센다.
  가상 스크롤 그리드는 렌더된 일부만 보인다.
- 완료 보고 전에 "총 N건 중 N건"을 모델 기준으로 다시 세어 확인한다.
- 이전 작업의 분류 결과를 근거로 **일괄 배제하지 않는다**. 분류 축이 다르면 누락이
  거기 몰린다. 배제는 항목 단위로 근거를 남기고, 재검토 시 "0건 추가"가 나와야 정당했던 것.
- 사용자가 쓴 원문(필기·노트·정리본)은 한 개념도 떨어뜨리지 않는다. 이미지·첨부도 개념이다.

**검증은 추측이 아니라 재현으로**
- "고쳤다"고 말하기 전에 수정 전 코드로 문제를 실제 재현하고, 수정 후 사라지는지 대조한다.
- 자동화 스크립트는 돌린 결과를 직접 확인한다. 도구가 "성공"이라 해도 산출물을 본다.

**배포·업로드는 완성본 한 번에**
- "일단 올려보고 문제 생기면 고치자"는 금지. 올리기 전 검증을 겹쳐 돌린다
  (정적 검사 + 실제 실행 + 시나리오 재현 + 상호작용 리뷰).
- 발견한 문제는 심각도별로 모아 **한 번에 수정 → 재검증 → 배포**.
- 배포 직전 무엇을 올리는지 보여주고 승인받는다.

**기존 사용자 데이터 안전 (실사용 앱 상시 규칙)**
기능 추가·수정마다 아래 4개를 점검하고 결과를 보고한다.
1. 구버전 캐시 공존 — 옛 파일을 캐시한 기기가 신버전 필드를 지우지 않는가
   (필드를 안 보낸 것 vs 비워서 보낸 것을 구분할 것)
2. 기존 저장 레코드 호환 — 기본값 병합이 기존 값을 덮지 않는가
3. 묘비/빈 값 — 값은 지웠는데 타임스탬프가 남으면 "삭제"로 전파된다. 값·ts·큐는 짝으로
4. 롤백 가능성 — 신버전이 쓴 데이터를 구버전이 읽어도 안 깨지는가

**토큰 절약**
- 큰 파일 전체 재독 금지. grep / 부분 읽기(offset-limit)로 변경 지점만 본다.
- 어디를 고쳤는지 사용자에게 물어 그 섹션만 보는 것도 좋다.

**삭제 금지 목록**
`SKILL.md` 및 그 폴더 · `*/outputs/` 산출물 · `~/.claude/plugins/` · `~/.claude/skills/`
· `*.bak-*` 백업. 정리 스크립트는 이 경로를 먼저 제외 목록으로 고정하고,
삭제 전 제외 목록을 사용자에게 보여주고 승인받는다.

**브라우저 자동화**
- 한글 타이핑은 IME 때문에 조용히 실패한다. ASCII 대체 경로를 미리 두고, 입력 후 실측 확인.
- 사이트 동작을 바꾸는 JS 주입은 자작 wrapper 말고 Userscripts(Safari 확장) 경로를 먼저 제안.

**도구 지정 존중**: 사용자가 도구를 지정하면(예: "NotebookLM으로") 임의로 대체하지 않는다.
제약이 요구사항과 충돌하면 먼저 알리고 확인받는다.

**위임** (Claude Code 전용): 토큰이 많이 들 작업은 Codex에 위임하되, 던지고 끝내지 않는다.
`pgrep -fl`로 실행 확인 → 로그 진전으로 생존 확인(CPU 평탄 ≠ hang) → 무진전이면 kill 후
원인 진단. 같은 방식 재시도 금지.
<!-- END:kmy-common -->
